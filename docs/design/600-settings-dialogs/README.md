# #600 designer reference — settings dialogs

Issue: https://github.com/Matysh/houseplan-card/issues/600
Оригинальное предложение и обсуждение: #591.

## Происхождение

Файлы этой папки — содержимое архива `houseplan-settings-design-2026-09-18.zip`,
приложенного к #591 18.09.2026.

```
SHA-256  06f07fc212947ee9da1a39aaab2d14cbd41154cc9effe39bc48e1c2e1517255b
```

Отпечаток сверен побайтово с вложением #591 20.09.2026. Он здесь не как след
разрешения, а как метка версии: если материалы обновятся, будет видно, из какой
редакции собрана папка.

Автор — @singlmolt-prog, дизайнер JustBusiness, сотрудник владельца репозитория.
Материал принадлежит команде владельца и лежит под лицензией репозитория на
общих основаниях (подтверждено владельцем 20.09.2026 в #600).

## Что здесь лежит

| Путь | Что это |
|---|---|
| `SPEC.md` | ТЗ автора на четыре диалога в структуре `PROCESS.md` §7.1: визуальная система §3.1, состав каждой карточки §4–§7, i18n §8, AC §11 |
| `IMPLEMENTATION-GUIDE.md` | где что в `src/`, порядок работ, маппинг контрол → ключ состояния, ветки, которых прототип не показывает |
| `OPEN-POINTS.md` | десять мест, где прототип задевает поведение; решения владельца по каждому — в #600 |
| `field-maps/FIELD-MAP-space.md` | карта полей Space settings: прототип → `SpaceDialogState` → ключ i18n → контрол |
| `ARCHIVE-README.md` | README автора из архива, как есть |
| `ISSUE-FORM.md` | текст, из которого заведён #591 |
| `reference/` | свёрстанный прототип всех четырёх форм |
| `screenshots/` | четыре формы целиком, 560 px, 2×, состояние по умолчанию |

## Как открыть прототип

```
cd docs/design/600-settings-dialogs/reference
node serve.mjs        # http://127.0.0.1:8135/
```

`file://` не работает: прототип собран на ES-модулях. Переключатель «Form» в
верхней панели показывает четыре формы. Данные форм лежат в `localStorage`
браузера; для чистого состояния открывать в приватном окне.

Windows-лаунчеры `START.cmd` и `START.ps1` из архива в репозиторий не
переносились — они ничего не добавляют к строке выше.

## Границы

- **Это дизайн-референс, а не продуктовый код.** JS прототипа не переносится:
  `localStorage`, демонстрационные пространства, устройства и списки остаются
  здесь. `reference/room-fill.test.mjs` — проверка автора на семантику заливок,
  CI её не запускает.
- **В бандл отсюда не попадает ничего.** Проверяется `bundle-tree`.
- **Приоритет при расхождении** (`SPEC.md` §2): ТЗ в теле #600 > действующие
  контракты House Plan (данные, безопасность, доступность, `hp-dialog`, touch) >
  визуал и взаимодействия прототипа > детали реализации прототипа.
- **Два решения владельца расходятся со `SPEC.md` автора** и имеют приоритет над
  ним: образцы карточки комнаты остаются (Q2), поле цвета получает новую
  раскладку, но прежний контрол `hp-color-opacity` без нативного пикера (Q5).

## Приёмка

[ACCEPTANCE.md](ACCEPTANCE.md) — парные кадры «референс / продукт» по каждому
диалогу и перечень принятых адаптаций. Документ пополняется по мере реализации и
**не заменяет** код-ревью на S7.
