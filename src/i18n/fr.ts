import dictionary from './fr.json' with { type: 'json' };

/** Must match the entry bundle before a lazy locale can be committed. */
export const fingerprint = '__HOUSEPLAN_SOURCE_FINGERPRINT__';

export { dictionary };
